on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    # - name: Use Node.js 16
    #   uses: actions/setup-node@v2
    #   with:
    #     node-version: '16'
      
    # - name: 🔨 Build Project
    #   run: |
    #     npm install
    #     npm run build
        
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: levarez.sytes.net:21
        username: ftp_varezdev_syes_net
        password: ${{ secrets.ftp_password }}